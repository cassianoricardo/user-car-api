#!/usr/bin/env groovy
pipeline {
    agent any
    tools {
        maven 'Maven 3.9.4'
        jdk 'jdk11'
    }
    stages {
        stage('Build') {
                    steps {
                        bat 'mvn -B -DskipTests clean package'
                    }
                }
    }
}