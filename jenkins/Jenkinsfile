#!/usr/bin/env groovy
pipeline {
    agent any
    tools {
        maven 'Maven 3.9.4'
        jdk 'OpenJDK'
    }
    stages {
        stage('Deployment') {
            steps {
             sh 'pid=\$(lsof -i:8989 -t); kill -TERM \$pid || kill -KILL \$pid'
             sh 'nohup ./mvnw spring-boot:run -Dserver.port=8081 &'
            }
        }
    }
}